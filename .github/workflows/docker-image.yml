name: Build and Push Docker Images

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Check out the repository
        uses: actions/checkout@v6

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Docker Container Registry
        uses: docker/login-action@v3
        with:
          registry: docker.io
          username: ${{ github.repository_owner }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build and push Docker image
        uses: docker/build-push-action@v4
        with:
          context: .
          cache-from: type=registry,ref=docker.io/${{ github.repository_owner }}/${{ github.repository }}:buildcache
          cache-to: type=registry,ref=docker.io/${{ github.repository_owner }}/${{ github.repository }}:buildcache,mode=max
          push: ${{ github.event_name == 'push' }}
          tags: |
            docker.io/${{ github.repository_owner }}/${{ github.repository }}:latest
            docker.io/${{ github.repository_owner }}/${{ github.repository }}:${{ github.sha }}
